import React from 'react';
import './Loading.css'


// i create a custom loading component to use on my pages while the data is being fetched

const Loading = () => {
    return ( 
        <div className='loader'>
            Loading
        </div>
     );
}
 
export default Loading;